#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n =0, i =2;
    if(n& (1<<i)){cout<<"ith bit is set"<<'\n';}
    else{cout<<"not set"<<'\n';}

    return 0;
}
